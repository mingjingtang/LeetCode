       /**
         * N02
         */
        Hashtable<Integer,Integer> b = new Hashtable<Integer, Integer>();
        int[] array = new int[2];
        
        for(int i = 0; i< numbers.length; i++){
            b.put(i,numbers[i]);
        }
        for(int i = 0; i< numbers.length; i++){
            if(b.containsValue((target-b.get(i)))
                array[0] = i;
                
                
                return array;
        }//for
    }
}